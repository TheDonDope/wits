# The GIFs are committed as ordinary blobs — they are a few hundred kilobytes
# and re-rendered rarely. The LFS filters that used to sit here described a
# setup that was never actually in use.
*.tape linguist-language=elixir
